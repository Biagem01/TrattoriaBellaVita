<!-- app/views/ingredienti/edit.html.erb -->
<%= stylesheet_link_tag 'edit_page', media: 'all', 'data-turbo-track': 'reload' %>
<div class="container mt-4">
  <h1 class="custom-title mb-4 text-center">Modifica Ingrediente</h1>

  <div class="card mx-auto">
    <div class="card-body">
      <%= form_with(model: @ingrediente, url: ingredienti_path(@ingrediente), local: true, html: { class: 'needs-validation', novalidate: true }) do |form| %>
        <% if @ingrediente.errors.any? %>
          <div id="error_explanation" class="alert alert-danger">
            <h2><%= pluralize(@ingrediente.errors.count, "errore") %> impedisce di salvare l'ingrediente:</h2>
            <ul>
              <% @ingrediente.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="mb-3">
          <%= form.label :nome, class: 'form-label' %>
          <%= form.text_field :nome, class: 'form-control', required: true %>
          <div class="invalid-feedback">
            Per favore, inserisci il nome dell'ingrediente.
          </div>
        </div>

        <div class="mb-3">
          <%= form.label :quantita, class: 'form-label' %>
          <%= form.number_field :quantita, class: 'form-control', required: true %>
          <div class="invalid-feedback">
            Per favore, inserisci la quantità.
          </div>
        </div>

        <div class="mb-3">
          <%= form.label :giacenza, class: 'form-label' %>
          <%= form.number_field :giacenza, class: 'form-control', required: true %>
          <div class="invalid-feedback">
            Per favore, inserisci la giacenza.
          </div>
        </div>

        <div class="d-grid mb-3">
          <%= form.submit 'Aggiorna Ingrediente', class: 'btn btn-primary' %>
        </div>
      <% end %>

      <div class="d-grid">
        <%= link_to 'Indietro', ingredienti_index_path, class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>

<!-- Option 1: Bootstrap Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
